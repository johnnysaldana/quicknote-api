{
  "name": "quicknote-app-server",
  "version": "1.0.0",
  "description": "A simple note-taking application that uses browser local storage for persistence.",
  "main": "index.js",
  "scripts": {
    "start": "serve -s build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "serve": "^12.0.1"
  }
}
